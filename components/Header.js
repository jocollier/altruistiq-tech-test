import styles from '../styles/Header.module.scss'

const Header = () => {
    return (
        <div className={styles.title}>
            <h1 className={styles.headerTitle}>Historical global carbon footprint</h1>
        </div>
    )
}

export default Header